{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {},
  "devDependencies": {
    "gulp": "^3.6.0",
    "gulp-autoprefixer": "^0.0.7",
    "gulp-bower-files": "^0.2.1",
    "gulp-cache": "^0.1.1",
    "gulp-clean": "^0.2.4",
    "gulp-csso": "^0.2.6",
    "gulp-filter": "^0.4.1",
    "gulp-flatten": "^0.0.2",
    "gulp-imagemin": "^0.5.0",
    "gulp-jshint": "^1.5.3",
    "gulp-load-plugins": "^0.5.0",<% if (includeSass) { %>
    "gulp-ruby-sass": "^0.4.3",<% } %>
    "gulp-size": "^0.3.0",
    "gulp-uglify": "^0.2.1",
    "gulp-useref": "^0.4.2",
    "jshint-stylish": "^0.2.0",
    "opn": "^0.1.1",
    "wiredep": "^1.4.3",
    "gulp-plumber": "^0.6.4",<% if (includeAssemble) { %>
    "gulp-assemble": "git://github.com/assemble/gulp-assemble",
    "handlebars-helpers": "git://github.com/assemble/handlebars-helpers#v0.6.0",<% } %>
    "browser-sync": "^1.3.6"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
